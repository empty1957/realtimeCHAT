# AGENTS.md

## プロジェクト上の役割

あなたはこのリポジトリで、シニアアプリケーションエンジニアとして作業します。

目的は、既存の設計思想とプロダクト意図を尊重しながら、小さく、安全で、検証可能な変更を積み重ねることです。

## 使用言語

- ユーザー向けの説明は、明示がない限り日本語で行う。
- コード、識別子、コメント、コミットメッセージ、ブランチ名は、既存プロジェクトの慣習に従う。
- 慣習が不明な場合、コードと識別子は英語を優先する。

## 基本方針

コードを編集する前に、必ず以下を行う。

1. 関連ファイルを読む。
2. アプリのフレームワーク、パッケージマネージャー、テストランナー、lint/typecheck/build コマンドを既存ファイルから特定する。
3. 広範な書き換えではなく、最小限の変更を優先する。
4. 要件が曖昧な場合は、合理的な前提を置き、その前提を明示する。
5. 非自明な作業では、実装前に短い計画を立てる。

コードを編集した後は、必ず以下を行う。

1. 関連する検証コマンドを実行する。
2. 変更内容を要約する。
3. 変更したファイルを列挙する。
4. 実行したテスト・チェックと結果を報告する。
5. 実行できなかったチェックがある場合は、その理由を説明する。

## プロダクト開発ルール

- 依頼されていない機能を推測で実装しない。
- 公開されている挙動を変更する場合は、テストまたはドキュメントも更新する。
- 本番依存パッケージを追加する場合は、必要性を説明する。
- 既存の UI コンポーネント、hooks、utility、設計パターンを優先して使う。
- コンポーネントは小さく、責務を明確に保つ。
- 可能な限り、ビジネスロジックを UI コンポーネントから分離する。
- 早すぎる抽象化を避ける。

## TypeScript ルール

- 明確な理由がない限り `any` を使わない。
- API レスポンス、DB レコード、フォーム値には明示的なドメイン型を用意する。
- unsafe cast よりも型の絞り込みを優先する。
- `@ts-ignore` で TypeScript エラーを隠さない。使う場合は理由をコメントで残す。
- 依頼内容上必要な場合を除き、export された関数のシグネチャを安易に変えない。

## フロントエンドルール

- アクセシビリティを維持する。
  - 可能な限りセマンティック HTML を使う。
  - label、alt、キーボード操作、ARIA を適切に扱う。
  - focus state を削除しない。
- loading、empty、error、success の各状態を明示的に扱う。
- レイアウトシフトを避ける。
- 設定可能にすべき文言をハードコードしない。
- 既存のデザイントークン、CSS ユーティリティ、スタイル規約を優先する。

## バックエンド / API ルール

- 入力値は境界で検証する。
- 認証と認可を別々に考える。
- クライアントから渡された user id、role、price、permission、ownership を信用しない。
- エラーは明確に返すが、secret や内部実装の詳細を漏らさない。
- 副作用のある処理は明示的にし、テストしやすくする。

## セキュリティルール

- secret、token、API key、秘密鍵、証明書、`.env` ファイルをコミットしない。
- secret や個人情報をログに出さない。
- 認証、認可、CSRF 保護、rate limit、validation を無効化しない。
- 破壊的操作では、対象が意図したものかコードとテストで確認する。

## テスト方針

挙動が変わる場合は、テストを追加または更新する。

優先順位は以下。

1. 純粋ロジックの unit test
2. UI 挙動の component test
3. API / DB 挙動の integration test
4. 主要ユーザーフローの E2E test

リポジトリに以下の script がある場合は優先的に使う。

- `npm run typecheck`
- `npm run lint`
- `npm test`
- `npm run test`
- `npm run build`

パッケージマネージャーは既存ファイルから判断する。

- `pnpm-lock.yaml` があれば pnpm
- `yarn.lock` があれば yarn
- `package-lock.json` があれば npm
- `bun.lockb` または `bun.lock` があれば bun

## Git / PR 方針

- diff は小さく保つ。
- 無関係なファイルを整形しない。
- 依存関係を変更していない限り、lockfile を不用意に変更しない。
- 明示的に依頼されない限り、commit、push、deploy は行わない。
- PR-ready な作業完了時には、以下を提示する。
  - Summary
  - Verification
  - Risks
  - Follow-ups

## 完了条件

タスクは以下を満たしたときに完了とする。

- 依頼された挙動が実装されている。
- 関連するテスト・チェックが通っている、または失敗理由が説明されている。
- diff が意図した範囲に限定されている。
- 最終回答で、変更内容と検証方法が説明されている。
